// Generated by CoffeeScript 1.4.0
(function() {

  $(function() {
    var denver, map, parseData, percentages, scaleControl;
    percentages = [];
    denver = [39.7334624, -104.9924559];
    map = L.map('map', {
      center: denver
    }).setView(denver, 14);
    scaleControl = new L.Control.Scale({
      position: 'bottomleft'
    });
    scaleControl.addTo(map);
    L.tileLayer('http://{s}.tile.cloudmade.com/BC9A493B41014CAABB98F0471D759707/997/256/{z}/{x}/{y}.png', {
      maxZoom: 18,
      attribution: 'Map data &copy; <a href="http://openstreetmap.org">OpenStreetMap</a> contributors, <a href="http://creativecommons.org/licenses/by-sa/2.0/">CC-BY-SA</a>, Imagery © <a href="http://cloudmade.com">CloudMade</a>'
    });
    
	L.tileLayer('http://{s}.tile.cloudmade.com/BC9A493B41014CAABB98F0471D759707/997/256/{z}/{x}/{y}.png', {
				
			}).addTo(map);


			L.marker(denver).addTo(map)
				.bindPopup("<b>Welcome to community votes!</b>").openPopup();

			// L.circle(denver, 500, {
			// 	color: 'red',
			// 	fillColor: '#f03',
			// 	fillOpacity: 0.5
			// }).addTo(map).bindPopup("I am a circle.");

			 L.polygon([
			 	[51.509, -0.08],
			 	[51.503, -0.06],
			 	[51.51, -0.047]
			 ]).addTo(map).bindPopup("I am a polygon !!!.");


			var popup = L.popup();

			function onMapClick(e) {
				popup
					.setLatLng(e.latlng)
					.setContent("You clicked the map at " + e.latlng.toString())
					.openOn(map);
			}

			map.on('click', onMapClick);
	;

    d3.csv("/dataset/projects.csv", function(err, data) {
      if (err) {
        throw err;
      }
      return parseData(data);
    });
    parseData = function(dataset) {
      var coor, d, desc, link, percentage, type, _i, _len, _results;
      _results = [];
      for (_i = 0, _len = dataset.length; _i < _len; _i++) {
        d = dataset[_i];
        if (d["Map coordinates"] === void 0) {
          continue;
        }
        coor = d["Map coordinates"].split(",");
        desc = d["description"];
        type = d["type"];
        link = d["link"];
        percentage = d['Percent'];
        L.marker(coor).addTo(map).bindPopup("<b>" + desc + "</b><br>" + type + "<br>" + "<a href='" + link + "'>" + "Taken from" + "</a>");
        _results.push(L.circle(coor, 250, {
          color: 'red',
          fillColor: '#f03',
          fillOpacity: 0.1
        }).addTo(map).bindPopup(percentage));
      }
      return _results;
    };
    return denver = [39.7334624, -104.9924559];
  });

}).call(this);
